<%= form_for([@appointment,@appointments_procedure]) do |f| %>
  <% if @appointments_procedure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointments_procedure.errors.count, "error") %> prohibited this appointments_procedure from being saved:</h2>

      <ul>
      <% @appointments_procedure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field campos-texto">
    <%= f.label :procedure_id, "Tratamiento" %><br>
    <%= f.collection_select :procedure_id, Procedure.all, :id, :nombre, { prompt: "Elije un tratamiento" }, class: "browser-default" %>
  </div>
  <div class="field campos-texto">
    <%= f.label :anotaciones %><br>
    <%= f.text_area :anotaciones %>
  </div>
  <div class="actions">
    <%= f.submit "Crear" %>
  </div>
<% end %>
